import React from 'react';
import PropTypes from 'prop-types';

const Select = props => {
    return (
        <div>
            
        </div>
    );
};

Select.propTypes = {
    
};

export default Select;